/**
  ******************************************************************************
  * @file    album.h
  * @brief   相册：浏览 SD 卡上的 128x64 图片（.BIN 页格式文件）
  *
  *          图片文件约定：
  *            - 位于 SD 卡根目录
  *            - 扩展名 .BIN（不区分大小写）
  *            - 内容为 1024 字节页格式位图（与 SSD1306 显存布局一致，
  *              可直接 SSD1306_ShowBitmap 显示）
  *
  *          操作：LT/RT 切换上一张/下一张，A 或 X 返回菜单
  ******************************************************************************
  */

#ifndef __ALBUM_H
#define __ALBUM_H

#include "stm32f10x.h"

/**
  * @brief  运行相册（阻塞，直到 A/X 返回）
  * @note   需要 SD 卡已挂载（FatFS）；无图片时显示提示
  */
void Album_Run(void);

#endif /* __ALBUM_H */
